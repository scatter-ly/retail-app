name: Test docker action release
on:
  release:
    types: [created]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: scatter-ly/test-action@main
        with:
          type: sbom
          url: http://example.com
      - name: Check for file
        run: ls -l
